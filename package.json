{
  "name": "maalfrid-initdb",
  "version": "0.4.1",
  "license": "Apache-2.0",
  "description": "Målfrid database initializer",
  "main": "index.js",
  "repository": "https://github.com/nlnwa/maalfrid-initdb",
  "private": true,
  "scripts": {
    "start": "node .",
    "lint": "standard",
    "build:docker": "docker build -t norsknettarkiv/maalfrid-initdb:$(git describe --tags --always) ."
  },
  "dependencies": {
    "rethinkdb-ts": "^2.4.0-rc.13"
  },
  "devDependencies": {
    "standard": "^12.0.1"
  }
}
